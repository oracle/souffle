
.decl gen(x: number)
gen(1).
gen(x+1) :- gen(x), !lim(x).

.decl gen2(x: number)
gen2(1).
gen2(x+1) :- gen2(x), !lim(x).

.decl lim(x : number)
lim(10000).

.decl transform2(x : number, y : number) eqrel output
transform2(x, y) :- gen(x), gen2(y).
