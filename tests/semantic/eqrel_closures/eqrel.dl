.decl A(x : number, y : number) eqrel output

A(1,2).
A(3,1).

A(6, 10).
A(8, 10).
A(10, 8).

// test implicit transitivity on explicit transitivity
.decl B(x : number, y : number) output
B(x, z) :- A(x, z).
B(x, z) :- A(x, y), B(y, z).

// unioning sets 
.decl C(x : number, y : number) output
C(x, y) :- A(x, x), A(y, y). 
